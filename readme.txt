if you want to train the HDNet with this project
1.change the data path of each dataset in dataloader:
     core/data/dataloader/XXX
     line 15  the root
2.the script for training is in the scripts/run.sh

if you want to test the module
1. the script for training is in the script/test.sh
2. the logs and the train models have been saved in the runs/